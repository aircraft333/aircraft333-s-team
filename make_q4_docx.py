# -*- coding: utf-8 -*-
"""生成《问题四 模型与工作流.docx》

与《问题三 模型与工作流.docx》同一套排版（共用 docx_kit）：
正文说明 + LaTeX 源码 + TeX Live 渲染的公式截图。

用法：python make_q4_docx.py
输出：问题四 模型与工作流.docx
"""
import os

from docx_kit import Doc

ROOT = os.path.dirname(os.path.abspath(__file__))
EQDIR = os.path.join(ROOT, "_docx_build", "eq")
OUT = os.path.join(ROOT, "问题四 模型与工作流.docx")

d = Doc("问题四 模型与工作流", EQDIR)

# =====================================================================
d.h1("一、问题描述与关键假设")

d.para("问题四要求：外网电价实际上也是实时波动的，需根据附件2 的小区负载与光伏实际发电、"
       "附件3 的光伏发电预报、附件4 的电价数据，在波动电价下重新计算问题二与问题三。")
d.para("也就是说，问题四并没有引入新的决策机制，而是把问题二、问题三的模型整体搬到"
       "“电价不再固定”的环境下，检验它们的适应性与经济性。")

d.h2("附件4 的数据特征（实测）")
d.item("形状 365 × 144，与附件1 逐时段一一对应；列均值恒等于附件1 的电价，最大绝对差仅 5.2e-5", "•")
d.item("全局 0.0076 ~ 1.7936 元/kWh，均值 0.7662 元/kWh", "•")
d.item("逐日均价在 0.5378 ~ 0.9290 之间漂移（标准差 0.1036），最高日与最低日相差 73%", "•")
d.item("日内峰谷比均值 5.74（附件1 固定电价下为 3.76）", "•")
d.para("由此可以确认附件4 的生成结构：电价 = 附件1 的分时形状 + 逐日整体水平漂移 + 逐槽噪声：")
d.eq(r"\frac{1}{365}\sum_{d=1}^{365}\pi_{d,t} \;\equiv\; \pi^{\mathrm{att1}}_t"
     r"\qquad\left(\max_t\left|\,\cdot\,\right| = 5.2\times 10^{-5}\right)")
d.eq(r"\mathrm{R}_d = \frac{\max_t \pi_{d,t}}{\min_t \pi_{d,t}},\qquad"
     r"\overline{\mathrm{R}} = \frac{1}{365}\sum_{d}\mathrm{R}_d = 5.74")
d.para("这一结构意味着两件事：日内「谷充峰放」的套利依然存在（甚至更有利，因为峰谷比更大），"
       "同时**新出现了跨日套利**的空间（在便宜的日期多充、在昂贵的日期多放）。")

d.h2("关键假设")
d.para("假设①：当天 0:00 已能获知当天的完整电价曲线（日前市场提前公布电价），故电价不需要"
       "预测，计划 LP 直接使用当天实际电价。这与题目「根据附件4 的电价数据」的表述一致。",
       indent=False)
d.para("假设②：除电价外，其余口径与问题二、问题三完全一致 —— 逐槽被动平衡执行、报童分位"
       "安全裕量、附件3 整点预报按点值口径插值。", indent=False)
d.note("这样处理的目的，是让「固定电价 → 波动电价」的差异可以完全归因于电价本身，"
       "而不是被其他口径变化污染。")

# =====================================================================
d.h1("二、对应问题二：0:00 计划 + 逐槽被动平衡")
d.para("模型与问题二完全同构，只是把固定的 π_t 换成逐日逐时段的 π_{d,t}。")
d.para("决策变量：b_t（购电量）、c_t（充电）、d_t（放电）、s_t（弃光）、E_t（储电量）。")
d.para("约束条件与问题二一致：")
d.item("日前功率平衡：", "1.")
d.eq(r"b_t + \hat{G}_{d,t} + d_t - c_t - s_t = \tilde{L}_{d,t},"
     r"\qquad \forall t = 1,\dots,144")
d.item("储能动态容量演化与初值、上下限、非负性：与问题二完全相同。", "2.")
d.para("目标函数：最小化单日计划购电费用（电价随时段变化）：", indent=False)
d.eq(r"\min\ \sum_{t=1}^{144}\pi_{d,t}\cdot b_t\cdot\Delta t")
d.para("安全裕量口径与问题二一致：逐时段取历史 30 天净负荷预测误差的 0.76 分位数。"
       "求解方式为逐日滚动，储电量逐日连续传递。")

# =====================================================================
d.h1("三、对应问题三：6:00 / 12:00 / 18:00 四阶段滚动调整")
d.para("模型与问题三完全同构，同样只是把 π_t 换成 π_{d,t}。购电费用函数变为：")
d.eq(r"C_t = \pi_{d,t}\min(b^{p}_t,\ b^{a}_t)"
     r" + 0.5\pi_{d,t}\left(b^{p}_t - b^{a}_t\right)^{+}"
     r" + 1.5\pi_{d,t}\left(b^{a}_t - b^{p}_t\right)^{+}")
d.para("线性化方式不变：b^a_t = b^p_t + δ⁺_t − δ⁻_t，C_t = π_{d,t}b^p_t + 1.5π_{d,t}δ⁺_t − 0.5π_{d,t}δ⁻_t；"
       "逐槽被动平衡规则同样通过凸分段线性约束精确嵌入 LP。")
d.para("滚动目标函数：", indent=False)
d.eq(r"\min\ \sum_{t=t_0}^{144}\left[\,C_t + 5\pi_{d,t}\,e_t\,\Delta t\,\right]")
d.para("其余口径与问题三一致：光伏预报取 40% 附件3 + 60% 前 3 天均值；负载预报为上周同一天"
       "叠加当日自适应修正（限幅 ±20%）；安全裕量为逐时段历史 60 天负载误差的 0.75 分位数；"
       "决策时刻 (0, 6, 12, 18)。")

# =====================================================================
d.h1("四、波动电价带来的新机会：跨日套利")
d.para("固定电价（附件1）下每一天的电价形状完全相同，储能无法把能量搬到另一天去获益，"
       "因此逐日独立决策与多日联合优化的差别极小 —— 实测只差 0.139%（见 q2_多日LP对比.txt）。")
d.para("波动电价（附件4）下逐日均价在 0.54 ~ 0.93 之间漂移，跨日套利空间被打开："
       "可以在便宜的日期多充电、在昂贵的日期多放电。此时理论上更完整的模型是"
       "**多日（滚动视界）联合优化**：")
d.eq(r"\min\ \sum_{d=d_0}^{d_0+K-1}\sum_{t=1}^{144}\pi_{d,t}\, b_{d,t}\,\Delta t")
d.para("其与逐日优化的唯一区别在于储电量不再要求「每日末回到初值」，而是在窗口内跨日连续：")
d.eq(r"E_{d,t} = E_{d,t-1} + \left(\eta\, c_{d,t} - \frac{d_{d,t}}{\eta}\right)\Delta t,"
     r"\qquad E_{d,0} = E_{d-1,\,144}")
d.para("为保证可比，对比时两侧都使用**实际**净负荷与**实际**电价，即不存在任何预测误差，"
       "差额就完全来自「决策视界」的不同。以 7 日窗口实测：", indent=False)
d.item("逐日 LP：1,629,250.9 元；7 日联合 LP：1,622,550.5 元", "•")
d.item("节省 6,700.4 元，相对 **0.411%**（固定电价下同口径仅为 0.139%，约 3 倍）", "•")
d.para("但绝对量仍然很小，根本原因是容量的硬约束：储能可用容量只有")
d.eq(r"E_{\max} - E_{\min} = 10800 - 1200 = 9600\ \mathrm{kWh}")
d.para("而日均净负荷约 7 万 kWh，两者相差近一个数量级。「跨日搬运能量」会被这个容量上限"
       "卡死，因此逐日 + 日内套利已经捕获了绝大部分价值。")
d.note("实用结论：波动电价下仍可采用逐日决策（即问题二、问题三的模型）；多日联合优化是"
       "理论上更完整的形式，实际增益有限，可作为模型的进一步讨论方向。")

# =====================================================================
d.h1("五、为什么波动电价反而更贵：受控分解")

d.para("这是一个容易被忽略的事实：附件4 在输出窗口（334 天）内的**逐时段均价 0.7575 "
       "反而低于**附件1 的固定电价 0.7662，但总费用却高了 4.91%。所以「附件4 更贵」这个"
       "直观解释是错的，必须把成因查清楚。")

d.para("为此做受控对照：两个口径共用同一套负载/光伏、同一套预报与安全裕量、同一套逐槽"
       "执行规则与链式推进方式，**唯一变量是电价矩阵**。把计划购电费按下式分解：")
d.eq(r"\sum_{d,t}\pi_{d,t}\,q_{d,t}\,\Delta t"
     r" = \underbrace{\sum_{d,t}\bar{\pi}_{t}\,q_{d,t}\,\Delta t}_{\text{mean-level term}}"
     r" + \underbrace{\sum_{d,t}\left(\pi_{d,t}-\bar{\pi}_{t}\right)q_{d,t}\,\Delta t}"
     r"_{\text{daily-drift term}}")
d.para("其中 π̄_t 为窗口内第 t 个时段的平均电价。实测结果（`q4_电价波动效应分解.txt`）：")
d.item("逐时段均价项：+16,966.3 元（2.8%）", "•")
d.item("**逐日漂移项：+585,294.0 元（97.2%）**", "•")
d.item("计划购电费合计 +602,260.3 元", "•")

d.para("也就是说，涨价几乎全部来自「逐日漂移」而不是「各时段平均更贵」。漂移项为什么是"
       "正的？看一下日购电量与当日电价水平的关系：")
d.eq(r"\mathrm{corr}\!\left(\sum_{t}q_{d,t},\ \bar{\pi}_d\right) = +0.9651")
d.para("**负载高的那些天，恰好也是电价整体偏高的天**。而负载是刚性的、必须满足，储能可用"
       "容量又只有 9600 kWh（日均购电 6.4 万 kWh，相差近一个数量级），无法跨日搬运能量，"
       "于是价格漂移就直接转化为费用上升。")

d.para("另外两个值得写进论文的定量事实：", indent=False)
d.item("逐日 LP 的再优化是有效的：把固定电价下的购电量按附件4 的电价计价要 13,883,703.9 元，"
       "而波动电价下 LP 实际只花 13,757,329.3 元，**扳回 126,374.6 元**。所以涨价不是求解或"
       "调度缺陷。", "•")
d.item("波动价下购电的时间分布被摊平：0:00 的购电占比从 12.29% 降到 8.36%；"
       "「购电均价 / 时间均价」从 0.801 升到 0.845，即择时能力略有下降，但这是次要因素。", "•")

# =====================================================================
d.h1("六、全年结果（供核对）")
d.para("2025-02-01 至 2025-12-31，共 334 天。两个口径都用同一套因果规则求解，保证结果可归因：",
       indent=False)
d.item("对应问题二：计划购电量 21,505,541.3 kWh；计划购电费 13,757,329.3 元；"
       "紧急购电量 146,514.8 kWh / 876,751.2 元；**总购电费 14,634,080.5 元**", "•")
d.item("对应问题三：计划购电量 20,969,438.2 kWh；调整后购电量 21,075,477.9 kWh（净 +106,039.7）；"
       "计划购电费 13,394,689.4 元；调整净增 257,672.3 元；紧急购电量 98,327.5 kWh / 558,593.0 元；"
       "**总购电费 14,210,954.8 元**", "•")
d.para("固定电价（附件1）与波动电价（附件4）的对比：", indent=False)
d.item("对应问题二：13,949,108.5 → 14,634,080.5 元，+684,972.0 元（+4.91%）", "1)")
d.item("对应问题三：13,589,362.4 → 14,210,954.8 元，+621,592.4 元（+4.57%）", "2)")
d.item("参考基准（无储能、按现货价直接购买净负荷）：16,407,319.6 → 17,026,782.9 元"
       "（+619,463.3 元，+3.78%）", "3)")
d.para("几点结论：", indent=False)
d.item("波动电价下问题三相对问题二仍省 423,125.7 元（2.89%），比固定电价下的 2.58% 还略高，"
       "说明多阶段滚动调整在电价波动时价值更大", "•")
d.item("实际平均购电单价从 0.6505 元/kWh 升到 0.6805 元/kWh；但注意**均价上升 4.27% 是在"
       "窗口时间均价下降 1.13% 的背景下发生的**，机理见第五节", "•")
d.item("储能相对无储能基准的节省：波动电价下 17,026,782.9 − 14,210,954.8 = 2,815,828.1 元"
       "（16.54%），储能的价值在波动电价下同样成立", "•")

d.save(OUT)
print("已生成：", OUT)
